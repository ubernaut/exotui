# Detailed development log: `<branch-name>`

- **Branch:** `<branch-name>`
- **Started:** YYYY-MM-DD

A chronological record of prompts, strategies, attempts, failures, and pivots for this branch. Preserve enough detail
that the same ground is not retrodden; link to task files, commits, or test output rather than duplicating them.

Each branch writes only to its own file, so parallel branches never conflict here. Replace `/` in the branch name with
`--` to form the filename: `feature/login` becomes `feature--login.md`.

## YYYY-MM-DD — Short description

### Prompt

<!-- What was asked or clarified. -->

### Response and strategy

<!-- What was done, why, what happened, and what remains. -->
