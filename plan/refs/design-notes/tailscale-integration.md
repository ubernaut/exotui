1. tree view showing computers in the network. this should use the deno_tui fileexplorer/tree view widget.

we should be able to open a remote shell on the computer from here via ssh. which will open a new terminal window here.

Possible features: drag and drop/paste a file from the local machine into the terminal. it should scp the file directly
into whatever folder is currenlty active in the shell on the remote machine.

under each machine in the tree menu yothere should be an actions menu which will let you launch things like a shell, or
a system monitor. another menu item under that should list tmux/exomux sessions clicking that will open that session in
a new shell or focus that window if it's already open
