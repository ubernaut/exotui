// Moved into the library (src/app/backgrounds/). This shim keeps the
// historical exomux names; everything here is a re-export.

export { ShellFireField as ExomuxFireField } from "@ubernaut/exotui";
export type {
  ShellFireFieldOptions as ExomuxFireFieldOptions,
  ShellFireInspection as ExomuxFireInspection,
} from "@ubernaut/exotui";
