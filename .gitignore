~*
node_modules/
.cache/
plan/NeonExodus/
.api-workbench-workspaces.json
docs/assets/*.map
# `deno task exomux:compile` output.
packages/exomux/exomux
# Butterchurn debug-mode logs (created only when the debug overlay is enabled).
logs/
